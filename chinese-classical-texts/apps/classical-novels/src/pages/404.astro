---
import Base from '../layouts/Base.astro';

const path = Astro.url.pathname;
---

<Base title="页面未找到">
  <div class="content-page mx-auto max-w-lg px-4 py-16 text-center">
    <h1 class="brand text-3xl" style="color: var(--primary)">404 · 页面未找到</h1>
    <p class="mt-3 text-sm text-muted">
      路径 <code class="rounded px-1.5 py-0.5" style="background: var(--paper-2)">{path}</code> 不存在或尚未生成。
    </p>
    <ul class="mt-6 space-y-2 text-sm">
      <li>
        本地开发请在 <code>chinese-classical-texts/apps/classical-novels</code> 下运行
        <code class="mx-1">npm run dev</code>，再访问
        <a class="hover:underline" style="color: var(--accent)" href="/">首页</a>。
      </li>
      <li>
        若刚改过内容，可试 <code>npm run dev:clean</code> 清缓存后重启。
      </li>
      <li>
        人物页入口：
        <a class="hover:underline" style="color: var(--accent)" href="/honglou/bestiary">红楼梦图鉴</a>
        ·
        <a class="hover:underline" style="color: var(--accent)" href="/honglou/graph">关系图谱</a>
      </li>
    </ul>
  </div>
</Base>
